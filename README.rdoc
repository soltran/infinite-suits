Clone of 99dresses.com. Implemented primarily in Backbone.js and served by a RESTful Rails API. Uses JQuery and JQuery UI to provide a more interactive user experience. Authentication is done with Devise. Features include image uploading to AWS, the use of modals, virtual currency, filtering.  
